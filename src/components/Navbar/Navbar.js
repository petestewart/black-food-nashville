import React from 'react';
// import PropTypes from 'prop-types';

import './Navbar.scss';

const Navbar = (props) => {
  return (
    <div className="Navbar">
      <h1>NAVBAR</h1>
    </div>
  );
};

// Navbar.propTypes = {}

export default Navbar;
