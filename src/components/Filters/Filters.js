import React from 'react';
// import PropTypes from 'prop-types';

import './Filters.scss';

const Filters = (props) => {
  return (
    <div className="Filters">
      <h1>FILTERS</h1>
    </div>
  );
};

// Filters.propTypes = {}

export default Filters;
